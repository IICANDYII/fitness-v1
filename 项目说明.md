Let me explore the project structure.

以下是项目的完整分析：

---

## 项目入口

项目通过 [start_dashboard.bat](start_dashboard.bat) 一键启动，会拉起两个 FastAPI 服务：

| 服务 | 端口 | 入口模块 | 访问地址 |
|------|------|----------|----------|
| **Dashboard API** | 8000 | `agent_service.reports.api:app` | `http://localhost:8000` |
| **Gym Analyzer API** | 8002 | `gym_analyzer.api:app` | `http://localhost:8002/analyzer` |

启动前会自动通过 Docker 启动 PostgreSQL 容器 `postgres-pgvector`。

---

## 页面一览

### Dashboard 服务 (端口 8000)

| 路由 | HTML 文件 | 说明 |
|------|-----------|------|
| `/` 或 `/report` | [training_dashboard.html](agent_service/reports/training_dashboard.html) | 训练仪表盘主页 |
| `/plan-viewer` | [plan_viewer.html](agent_service/reports/plan_viewer.html) | 训练计划查看/生成器 |

其他 HTML 页面（通过直接访问文件）：
- [muscle_input.html](agent_service/reports/muscle_input.html) — 肌群输入
- [muscle_viewer.html](agent_service/reports/muscle_viewer.html) — 肌群查看器
- [hr_detail.html](agent_service/reports/hr_detail.html) — 心率详情
- [index.html](agent_service/reports/index.html) — 报告首页

### Gym Analyzer 服务 (端口 8002)

| 路由 | HTML 文件 | 说明 |
|------|-----------|------|
| `/` 或 `/analyzer` | [analyzer.html](gym_analyzer/analyzer.html) | 视频分析器页面 |

---

## 功能模块

### 1. 训练仪表盘 (Dashboard)
- **每日分析** (`/api/daily`) — 训练时长、热量消耗、完成率、肌群分布热力图、训练平衡评分
- **周报** (`/api/weekly`) — 近7日训练量/热量汇总、每日组数折线图、训练频率、肌群覆盖
- **肌群热力图** (`/api/muscles`) — SVG 人体肌群高/中/低强度可视化
- **月历** (`/api/calendar`) — 按月展示每日训练状态 (full/partial)
- **心率详情** (`/api/hr_detail`) — 生物特征流 (心率/HRV/疲劳) + 动作执行记录
- **用户管理** (`/api/users`, `/api/user`) — 多用户切换，按性别显示不同人体图

### 2. 训练计划 (Plan Viewer)
- **查看计划** (`/api/plan-viewer/latest-plan`) — 加载最新训练计划（文件缓存优先，DB 回退）
- **用户画像** (`/api/plan-viewer/profile`) — 查看/编辑长期用户画像
- **计划生成** (`/api/plan-viewer/regenerate-plan`) — 异步 AI 生成训练计划 (支持 v2/v3 版本)
- **Prompt 管理** — 切换/查看 v2/v3 prompt 版本
- **肌群 SVG** (`/api/plan-viewer/muscle-svg/{side}`) — 服务端渲染高亮肌群 SVG

### 3. 视频分析器 (Gym Analyzer)
- **视频上传** (`POST /api/upload`) — 上传健身视频
- **训练计划上传** (`POST /api/upload/plan`) — 上传训练计划 JSON
- **文件列表** (`/api/files`) — 列出 input/ 下的视频和心率文件
- **触发分析** (`POST /api/analyze`) — 后台触发 AI 视频分析（Gemini 模型识别动作）
- **分析进度** (`/api/analyze/progress`) — 实时查询分析进度
- **用户管理** — 创建/列出用户

---

## 数据库信息

### 数据库类型
**PostgreSQL + pgvector** (通过 Docker 容器 `postgres-pgvector` 运行)

### 连接配置
| 参数 | 默认值 | 环境变量 |
|------|--------|----------|
| Host | `localhost` | `DB_HOST` / `POSTGRES_HOST` |
| Port | `5432` | `DB_PORT` / `POSTGRES_PORT` |
| Database | `fitness` | `DB_NAME` / `POSTGRES_DB` |
| User | `postgres` | `DB_USER` / `POSTGRES_USER` |
| Password | `666666` | `DB_PASSWORD` / `POSTGRES_PASSWORD` |

### 涉及的数据库表

| 表名 | 用途 |
|------|------|
| `user_profile_long_term` | 用户长期画像 (性别/年龄/身高/体重/目标等) |
| `user_profile` | 用户基本信息 (FK 关联) |
| `workout_session` | 训练会话记录 (时间/热量/完成率/总训练量) |
| `exercise_execution` | 动作执行明细 (组数/次数/节奏/ROM/置信度/时长) |
| `biometric_stream` | 生物特征流 (心率/HRV/疲劳/步数) |
| `exercises` | 动作库 (名称/肌群映射/MET值/难度) |
| `workout_plan` | 训练计划 (JSON 存储) |

### 其他外部依赖
- **Gemini API** — 视频分析 AI 模型，通过 `NEXTROUTER_BASE_URL` / `NEXTROUTER_API_KEY` 配置
- **Docker** — 运行 PostgreSQL 容器