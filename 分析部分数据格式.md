# 训练分析仪表板 — API 数据格式说明

> 前端基地址：`http://localhost:8002/api`  
> 所有接口均为 `GET` 请求，返回 `Content-Type: application/json`。

---

## 接口总览

| 接口 | 用途 | 对应卡片 |
|------|------|----------|
| `GET /api/daily` | 当日训练统计 | 卡片 1 — 当日分析 |
| `GET /api/muscles` | 肌群训练热力等级 | 卡片 2 — 训练部位可视化 |
| `GET /api/weekly` | 本周训练汇总 + 折线图 | 卡片 3 — 每周分析 |
| `GET /api/calendar` | 月度训练日历 | 卡片 4 — 月历视图 |

---

## 1. `GET /api/daily` — 当日分析

### 响应结构

```json
{
  "duration_min": 75,
  "calories": 560,
  "avg_hr": 132,
  "completion_rate": 87,
  "muscle_distribution": {
    "胸": 85,
    "背": 70,
    "肩": 60,
    "二头": 45,
    "腹": 30
  }
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `duration_min` | `number` | ✅ | 训练时长，单位：分钟 |
| `calories` | `number` | ✅ | 消耗热量，单位：kcal |
| `avg_hr` | `number \| null` | ❌ | 平均心率，单位：bpm；无数据时可传 `null`，显示为 `--` |
| `completion_rate` | `number` | ✅ | 完成率，范围 `0–100`，单位：% |
| `muscle_distribution` | `object` | ✅ | 各肌群训练量占比，键为显示名称（中文），值为百分比数字 `0–100` |

### `muscle_distribution` 颜色映射规则

| 百分比范围 | 颜色 | 含义 |
|-----------|------|------|
| ≥ 60 | 绿色 `#10b981` | 充分训练 |
| 30 – 59 | 橙色 `#f59e0b` | 中等训练 |
| < 30 | 红色 `#ef4444` | 训练不足 |

---

## 2. `GET /api/muscles` — 训练部位可视化热力图

### 响应结构

```json
{
  "chest": "high",
  "abdominals": "medium",
  "obliques": "low",
  "front-shoulders": "high",
  "rear-shoulders": "medium",
  "traps": "low",
  "traps-middle": "medium",
  "biceps": "high",
  "triceps": "medium",
  "forearms": "low",
  "lats": "high",
  "lowerback": "medium",
  "quads": "low",
  "hamstrings": "medium",
  "glutes": "high",
  "calves": "low"
}
```

### 字段说明

- **键（key）**：SVG 中肌群组的 `id` 属性，见下方可用肌群列表
- **值（value）**：训练强度等级，枚举值 `"high"` | `"medium"` | `"low"`
- 未出现在响应中的肌群保持默认灰色 `rgb(184,188,204)`

### 强度等级颜色映射

| 值 | 颜色 | 说明 |
|----|------|------|
| `"high"` | 红色 `#F04034` | 高强度（图例：高） |
| `"medium"` | 绿色 `#52C41A` | 中等强度（图例：中） |
| `"low"` | 浅蓝 `#9FC6FF` | 低强度（图例：低） |

### 可用肌群 ID 列表

| ID | 中文名 | 所在视图 |
|----|--------|----------|
| `chest` | 胸部 | 正面 |
| `abdominals` | 腹部 | 正面 |
| `obliques` | 腹斜肌 | 正面 |
| `front-shoulders` | 前肩 | 正面 |
| `biceps` | 肱二头肌 | 正面 |
| `forearms` | 前臂 | 正/背面 |
| `quads` | 股四头肌 | 正面 |
| `calves` | 小腿 | 正/背面 |
| `traps` | 斜方肌（上） | 正/背面 |
| `traps-middle` | 斜方肌（中） | 背面 |
| `rear-shoulders` | 后肩 | 背面 |
| `triceps` | 肱三头肌 | 背面 |
| `lats` | 背阔肌 | 背面 |
| `lowerback` | 下背 | 背面 |
| `hamstrings` | 腘绳肌 | 背面 |
| `glutes` | 臀部 | 背面 |

> **注意**：SVG 中关节点（`shoulders`、`elbow`、`wrist`、`hips`、`knees`、`ankles`、`upper-spine`、`scapula`、`lower-spine`）带有 `class="hidden"`，前端已强制隐藏，**无需**在此接口中返回。

---

## 3. `GET /api/weekly` — 每周分析

### 响应结构

```json
{
  "total_sets": 220,
  "sets_trend_pct": 1.12,
  "total_calories": 3860,
  "calories_trend_pct": -8,
  "daily_sets": [22, 45, 38, 41, 50, 14, 10],
  "day_labels": ["周一", "周二", "周三", "周四", "周五", "周六", "周日"]
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `total_sets` | `number` | ✅ | 本周总训练量，单位：组 |
| `sets_trend_pct` | `number` | ✅ | 组数环比变化率；正数显示绿色上箭头，负数显示红色下箭头，`0` 显示 `--` |
| `total_calories` | `number` | ✅ | 本周总消耗热量，单位：kcal |
| `calories_trend_pct` | `number` | ✅ | 热量环比变化率；正负含义同上 |
| `daily_sets` | `number[]` | ✅ | 每日组数数组，长度为 7，对应折线图各点；顺序需与 `day_labels` 一一对应 |
| `day_labels` | `string[]` | ✅ | X 轴日期标签数组，长度为 7，如 `["周一", ..., "周日"]` 或实际日期 `["5/20", ...]` |

### 趋势显示规则

| `trend_pct` 值 | 显示效果 |
|---------------|---------|
| `> 0` | `↑{n}%`（绿色） |
| `< 0` | `↓{n}%`（红色，取绝对值） |
| `= 0` | `--`（灰色） |

---

## 4. `GET /api/calendar` — 月历视图

### 响应结构

```json
{
  "year": 2024,
  "month": 5,
  "days": {
    "2024-05-02": "full",
    "2024-05-03": "full",
    "2024-05-07": "partial",
    "2024-05-08": "full",
    "2024-05-09": "full",
    "2024-05-10": "full",
    "2024-05-11": "full",
    "2024-05-14": "partial",
    "2024-05-15": "full",
    "2024-05-16": "full",
    "2024-05-17": "full",
    "2024-05-21": "partial",
    "2024-05-22": "full",
    "2024-05-23": "full",
    "2024-05-24": "full",
    "2024-05-25": "partial"
  }
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `year` | `number` | ✅ | 日历所显示的年份，如 `2024` |
| `month` | `number` | ✅ | 日历所显示的月份，范围 `1–12` |
| `days` | `object` | ✅ | 训练日状态映射；键为 `"YYYY-MM-DD"` 格式日期字符串，值为训练状态；**未出现的日期视为无训练** |

### `days` 状态枚举

| 值 | 样式 | 含义 |
|----|------|------|
| `"full"` | 实心绿色圆圈 | 完整完成当日训练计划 |
| `"partial"` | 绿色边框空心圆 | 部分完成当日训练计划 |
| *(键不存在)* | 普通数字 | 当日无训练记录 |

> **今日**（前端根据系统时间自动判断）显示为实心蓝色圆圈 `#3b82f6`，无论 `days` 中是否有该日期，优先级最高。

---

## 附：完整响应示例（合并版）

```json
{
  "daily": {
    "duration_min": 75,
    "calories": 560,
    "avg_hr": 132,
    "completion_rate": 87,
    "muscle_distribution": {
      "胸": 85,
      "背": 70,
      "肩": 60,
      "二头": 45,
      "腹": 30
    }
  },
  "muscles": {
    "chest": "high",
    "lats": "high",
    "front-shoulders": "medium",
    "rear-shoulders": "medium",
    "abdominals": "low",
    "quads": "low"
  },
  "weekly": {
    "total_sets": 220,
    "sets_trend_pct": 1.12,
    "total_calories": 3860,
    "calories_trend_pct": -8,
    "daily_sets": [22, 45, 38, 41, 50, 14, 10],
    "day_labels": ["周一", "周二", "周三", "周四", "周五", "周六", "周日"]
  },
  "calendar": {
    "year": 2024,
    "month": 5,
    "days": {
      "2024-05-02": "full",
      "2024-05-07": "partial"
    }
  }
}
```
